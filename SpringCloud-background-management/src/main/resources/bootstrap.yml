spring:
  cloud:
    config:
      profile: dev
      name: neo-config
      discovery:
        service-id: SpringClound-config
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
management:
  endpoint:
    web:
      exposure:
        include: refresh